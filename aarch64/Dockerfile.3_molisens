ARG DOCKER_BASE=ghcr.io/molisens-mosep/humble:dependencies_aarch64
FROM $DOCKER_BASE      

SHELL ["/bin/bash", "-o", "pipefail", "-c"]

COPY aarch64/env.sh /etc/profile.d/env.sh
COPY aarch64/entrypoint /entrypoint

ENTRYPOINT ["/entrypoint"]
CMD ["/bin/sh", "-c", "trap 'exit 147' TERM; tail -f /dev/null & while wait ${!}; [ $? -ge 128 ]; do true; done"]


